name: pinger_internet_status
description: A Flutter package for monitoring internet connectivity by periodically pinging specified URLs.
version: 0.2.1
homepage: https://github.com/TrachukV/pinger_internet_status-main

environment:
  sdk: '>=3.2.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
